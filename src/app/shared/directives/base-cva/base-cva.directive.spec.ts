import { BaseCVADirective } from './base-cva.directive';

describe('BaseCVADirective', () => {
  it('should create an instance', () => {
    const directive = new BaseCVADirective();
    expect(directive).toBeTruthy();
  });
});
