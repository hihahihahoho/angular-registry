import { ReplaceToStyleContentFromServerPipe } from './replace-to-style-content-from-server.pipe';

describe('ReplaceToStyleContentFromServerPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceToStyleContentFromServerPipe();
    expect(pipe).toBeTruthy();
  });
});
